
#include <minecraft_protodef.h>
#include <lib/except.h>

err_t process_status_packet_ping(client_t* client, status_packet_ping_t* packet) {
    err_t err = NO_ERROR;

    CHECK_FAIL("TODO: process_status_packet_ping");

cleanup:
    return err;
}
