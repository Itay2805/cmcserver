
#include <minecraft_protodef.h>
#include <lib/except.h>

err_t process_status_packet_ping_start(client_t* client, status_packet_ping_start_t* packet) {
    err_t err = NO_ERROR;

    CHECK_FAIL("TODO: process_status_packet_ping_start");

cleanup:
    return err;
}

err_t process_login_packet_login_start(client_t* client, login_packet_login_start_t* packet) {
    err_t err = NO_ERROR;

    CHECK_FAIL("TODO: process_login_packet_login_start");

cleanup:
    return err;
}

err_t process_login_packet_login_plugin_response(client_t* client, login_packet_login_plugin_response_t* packet) {
    err_t err = NO_ERROR;

    CHECK_FAIL("TODO: process_login_packet_login_plugin_response");

cleanup:
    return err;
}

err_t process_login_packet_encryption_begin(client_t* client, login_packet_encryption_begin_t* packet) {
    err_t err = NO_ERROR;

    CHECK_FAIL("TODO: process_login_packet_encryption_begin");

cleanup:
    return err;
}

